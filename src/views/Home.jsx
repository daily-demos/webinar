import React from "react";
import styled from "styled-components";
import theme from "../theme";

const Home = () => {
  return <FlexContainer>hi</FlexContainer>;
};

const FlexContainer = styled.div`
  display: flex;
  align-items: stretch;
  flex-wrap: wrap;
`;

export default Home;
