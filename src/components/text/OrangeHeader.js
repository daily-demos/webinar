import styled from "styled-components";
import theme from "../../theme";

const OrangeHeader = styled.p`
  font-size: ${theme.fontSize.large};
  color: ${theme.colors.orange};
  padding-top: 0.25rem;
  padding-bottom: 0.5rem;
  margin: 0;
`;

export default OrangeHeader;
